{
  "name": "weather-slack-app",
  "version": "0.1.0",
  "bin": {
    "weather-slack-app": "bin/weather-slack-app.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "lint": "npx standard .",
    "lint:fix": "npx standard --fix .",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.73.0",
    "jest": "^29.5.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.73.0",
    "constructs": "^10.0.0",
    "standard": "^17.0.0"
  }
}
